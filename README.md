# 📸 ESP32-CAM Автоматическая Фотосъемка с Загрузкой на Сервер 🌐

![ESP32](https://img.shields.io/badge/ESP32-FF6600?style=for-the-badge&logo=espressif&logoColor=white)
![Arduino](https://img.shields.io/badge/Arduino-00979D?style=for-the-badge&logo=arduino&logoColor=white)
![PlatformIO](https://img.shields.io/badge/PlatformIO-1A1A1A?style=for-the-badge&logo=platformio&logoColor=00A8FF)
![License](https://img.shields.io/github/license/f2re/esp32cam?style=for-the-badge)
![Issues](https://img.shields.io/github/issues/f2re/esp32cam?style=for-the-badge)
![Forks](https://img.shields.io/github/forks/f2re/esp32cam?style=for-the-badge)
![Stars](https://img.shields.io/github/stars/f2re/esp32cam?style=for-the-badge)

## 📖 Описание проекта

**ESP32-CAM Автоматическая Фотосъемка** — это проект, который позволяет автоматически захватывать фотографии с помощью камеры ESP32-CAM и загружать их на удаленный сервер. Устройство работает автономно, делая снимки по расписанию каждые 10 минут, затем переходит в режим глубокого сна для экономии энергии. После пробуждения устройство подключается к Wi-Fi, делает снимок и отправляет его на сервер.

### 🎯 Основные возможности

- 📷 Автоматическое фотографирование с ESP32-CAM (OV2640)
- 📤 Загрузка фотографий на HTTP-сервер методом POST
- ⏰ Автоматическая съемка по расписанию (по умолчанию каждые 10 минут)
- 😴 Режим глубокого сна между съемками для экономии энергии
- 🌐 Подключение к Wi-Fi для передачи данных
- 🎨 Настройка параметров камеры (качество, формат, баланс белого)
- 🔄 Автоматическая повторная попытка при неудачной загрузке
- 📶 Индикация состояния с помощью светодиодов

### 🛠️ Технические характеристики

- ✅ Микроконтроллер: ESP32 (на плате ESP32-CAM)
- ✅ Камера: OV2640 (до разрешения UXGA 1600x1200)
- ✅ Память: PSRAM (необходима для работы камеры)
- ✅ Интерфейс: UART, Wi-Fi
- ✅ Питание: 5V USB или 3.3V внешний источник
- ✅ Потребление: минимальное в режиме сна (~10μA)

## 🚀 Быстрый старт

### 📋 Необходимые компоненты

- [x] ESP32-CAM модуль (AI-Thinker с OV2640)
- [x] Micro-USB кабель для программирования
- [x] Компьютер с установленным PlatformIO
- [x] Wi-Fi сеть (или точка доступа Raspberry Pi)
- [x] HTTP-сервер для приема фотографий

### 💻 Установка и настройка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/f2re/esp32cam.git
   cd esp32cam
   ```

2. **Установите зависимости PlatformIO:**
   ```bash
   pio run
   ```

3. **Настройте параметры подключения (в файле src/OV-2640 (120).cpp):**
   - SSID и пароль Wi-Fi сети
   - IP-адрес и порт сервера
   - ID камеры (для идентификации на сервере)
   - Интервал между съемками

4. **Загрузите прошивку:**
   ```bash
   pio run --target upload
   ```

5. **Проверьте работу:**
   ```bash
   pio device monitor
   ```

## 📥 Подробная инструкция по установке и прошивке

### Подготовка среды разработки

1. **Установите Visual Studio Code** (если еще не установлен):
   - Скачайте и установите с официального сайта: https://code.visualstudio.com/

2. **Установите расширение PlatformIO**:
   - Откройте VS Code
   - Перейдите в раздел "Extensions" (Ctrl+Shift+X)
   - Найдите и установите "PlatformIO IDE"

3. **Альтернативно, установите PlatformIO Core (CLI)**:
   - Установите Python (версия 3.6 или выше)
   - Установите PlatformIO Core:
     ```bash
     pip install --upgrade platformio
     ```

### Подготовка оборудования

1. **Подключите ESP32-CAM к компьютеру**:
   - Используйте Micro-USB кабель для подключения модуля к компьютеру
   - Убедитесь, что модуль получает питание и индикатор питания горит

2. **Установите драйвера (если необходимо)**:
   - Для Windows: драйвера CP210x для USB-UART адаптера
   - Для Linux/Mac: драйвера обычно уже установлены

### Настройка проекта

1. **Откройте проект в PlatformIO**:
   - В VS Code: "File" → "Open Folder" → выберите папку с проектом
   - Или в терминале: `cd esp32cam && code .`

2. **Проверьте конфигурацию в `platformio.ini`**:
   - Убедитесь, что указана правильная плата (`board = esp32cam`)
   - Проверьте настройки скорости монитора и загрузки

3. **Настройте параметры в прошивке (`src/OV-2640 (120).cpp`)**:
   - Измените `WIFI_SSID` и `WIFI_PASS` на свои параметры Wi-Fi
   - Укажите правильный `SERVER_HOST`, `SERVER_PORT` и `SERVER_PATH`
   - При необходимости измените `CAM_ID` и `CAPTURE_PERIOD_SEC`

### Компиляция и загрузка прошивки

1. **Проверьте проект**:
   ```bash
   pio check
   ```

2. **Скомпилируйте проект**:
   ```bash
   pio run
   ```

3. **Загрузите прошивку в ESP32-CAM**:
   ```bash
   pio run --target upload
   ```

   Или используйте кнопку "Upload" в PlatformIO IDE

4. **Проверьте работу устройства**:
   ```bash
   pio device monitor
   ```

   Или откройте Serial Monitor в PlatformIO IDE

### Альтернативный способ загрузки через терминал

Если возникают проблемы с загрузкой через PlatformIO IDE, попробуйте использовать командную строку:

```bash
# Полная очистка и пересборка проекта
pio run --clean
pio run

# Загрузка прошивки
pio run --target upload --upload-port /dev/ttyUSB0  # Linux
pio run --target upload --upload-port COM3          # Windows
```

Замените `/dev/ttyUSB0` или `COM3` на соответствующий порт вашего устройства (его можно посмотреть в PlatformIO IDE в правом нижнем углу).

### Проверка работы устройства

После успешной загрузки прошивки:

1. **Откройте Serial Monitor**:
   - В PlatformIO IDE: "Serial Monitor" или команда `pio device monitor`
   - Установите скорость 115200 бод

2. **Наблюдайте за процессом**:
   - Устройство должно подключиться к Wi-Fi
   - Сделать снимок
   - Отправить его на сервер
   - Перейти в режим сна

3. **Проверьте результат**:
   - Убедитесь, что изображения поступают на сервер
   - Проверьте логи сервера
   - Убедитесь, что на сервере создаются файлы изображений

## 📁 Структура проекта

```
esp32cam/
├── src/
│   └── OV-2640 (120).cpp    # Основной файл прошивки
├── include/                 # Заголовочные файлы
├── lib/                     # Библиотеки
├── test/                    # Тесты
├── platformio.ini           # Конфигурация PlatformIO
├── main_by_laureat          # Альтернативная версия прошивки
├── .gitignore               # Файлы, игнорируемые Git
├── README.md                # Документация (текущий файл)
├── LICENSE.md               # Лицензия
├── CONTRIBUTING.md          # Руководство по участию
└── CODE_OF_CONDUCT.md       # Кодекс поведения
```

## 💡 Принцип работы

После загрузки прошивки устройство выполняет следующий цикл:

1. **Инициализация**: запуск последовательного порта, настройка светодиодов, инициализация камеры
2. **Подключение к Wi-Fi**: подключение к заданной точке доступа
3. **Съемка**: захват изображения с камеры
4. **Загрузка**: отправка изображения на HTTP-сервер методом POST в формате multipart/form-data
5. **Режим сна**: переход в режим глубокого сна на заданный интервал (по умолчанию 10 минут)
6. **Повтор**: после пробуждения цикл повторяется

### 🔧 Настройка параметров

В файле `src/OV-2640 (120).cpp` можно настроить следующие параметры:

#### Параметры подключения
```cpp
// Параметры Wi-Fi
static const char* WIFI_SSID = "Raspberry";      // Имя точки доступа Wi-Fi
static const char* WIFI_PASS = "12345678";      // Пароль Wi-Fi

// Параметры сервера
static const char* SERVER_HOST = "192.168.4.1"; // IP-адрес сервера
static const uint16_t SERVER_PORT = 8000;       // Порт сервера
static const char* SERVER_PATH = "/upload";     // Путь для загрузки изображений

// ID камеры для раздельных папок на сервере
static const char* CAM_ID = "cam3";             // Идентификатор камеры
```

#### Параметры таймингов
```cpp
// Интервалы
static const uint32_t CAPTURE_PERIOD_SEC = 600;   // 10 минут между съемками
static const uint32_t FAIL_SLEEP_SEC     = 600;   // время ожидания при неудаче
static const uint8_t  UPLOAD_RETRIES     = 3;     // количество попыток загрузки
```

#### Пин-конфигурация
```cpp
// Пины
#define LED_FLASH_GPIO    GPIO_NUM_4      // GPIO4 - вспышка
#define STATUS_LED_GPIO   GPIO_NUM_33     // GPIO33 - индикаторный светодиод
```

#### Параметры камеры
В функции `applyOv2640QualityProfile()` можно настроить параметры камеры:
```cpp
// Настройки качества для OV2640
s->set_brightness(s, 0);       // Яркость: -2..2
s->set_contrast(s, 0);         // Контраст: -2..2
s->set_saturation(s, 0);       // Насыщенность: -2..2
s->set_whitebal(s, 1);         // Баланс белого (вкл/выкл)
s->set_awb_gain(s, 1);         // Автоматический баланс белого (вкл/выкл)
s->set_exposure_ctrl(s, 1);    // Контроль экспозиции (вкл/выкл)
s->set_gain_ctrl(s, 1);        // Контроль усиления (вкл/выкл)
s->set_lenc(s, 1);             // Коррекция линз (вкл/выкл)
s->set_raw_gma(s, 1);          // Гамма-коррекция (вкл/выкл)
s->set_wpc(s, 1);              // Коррекция белых пикселей (вкл/выкл)
s->set_bpc(s, 1);              // Коррекция плохих пикселей (вкл/выкл)
s->set_dcw(s, 1);              // Downsize/crop pipeline (вкл/выкл)
s->set_aec2(s, 0);             // AEC2 алгоритм (вкл/выкл)
s->set_gainceiling(s, (gainceiling_t)2); // Ограничение усиления
```

#### Параметры инициализации камеры
```cpp
config.xclk_freq_hz = 20000000;     // Частота XCLK (обычно 20MHz для OV2640)
config.pixel_format = PIXFORMAT_JPEG; // Формат пикселей (JPEG)
config.frame_size   = FRAMESIZE_UXGA; // Размер кадра (UXGA 1600x1200)
config.jpeg_quality = 10;            // Качество JPEG (меньше = лучше)
config.fb_count     = 2;             // Количество буферов кадров
config.fb_location  = CAMERA_FB_IN_PSRAM; // Расположение буфера (в PSRAM)
config.grab_mode    = CAMERA_GRAB_LATEST; // Режим захвата
```

## ⚙️ Настройка проекта

Файл `platformio.ini` содержит конфигурацию для сборки проекта:

```ini
[env:esp32cam]
platform = espressif32
board = esp32cam
framework = arduino
build_flags =
  -DBOARD_HAS_PSRAM
  -mfix-esp32-psram-cache-issue
  -DCORE_DEBUG_LEVEL=1
monitor_speed = 115200
```

### Параметры конфигурации PlatformIO

- `platform = espressif32` - платформа для ESP32 микроконтроллеров
- `board = esp32cam` - тип платы (ESP32-CAM от AI-Thinker)
- `framework = arduino` - используемый фреймворк (Arduino)
- `build_flags` - флаги компиляции:
  - `-DBOARD_HAS_PSRAM` - определяет наличие PSRAM на плате
  - `-mfix-esp32-psram-cache-issue` - исправляет проблему с кэшем PSRAM
  - `-DCORE_DEBUG_LEVEL=1` - уровень отладки ядра
- `monitor_speed = 115200` - скорость последовательного порта для мониторинга

## 📡 Протокол передачи данных

Устройство отправляет изображения на сервер методом HTTP POST в формате multipart/form-data:
- Поле `cam_id`: идентификатор камеры
- Поле `file`: сам файл изображения в формате JPEG

Пример запроса:
```
POST /upload HTTP/1.1
Host: 192.168.4.1:8000
Content-Type: multipart/form-data; boundary=----esp32camBoundary7MA4YWxk
Content-Length: [размер данных]

------esp32camBoundary7MA4YWxk
Content-Disposition: form-data; name="cam_id"

cam3
------esp32camBoundary7MA4YWxk
Content-Disposition: form-data; name="file"; filename="frame.jpg"
Content-Type: image/jpeg

[двоичные данные изображения]
------esp32camBoundary7MA4YWxk--
```

## 🔌 Подключение оборудования

Для правильной работы устройства необходимо использовать следующую распиновку (для AI-Thinker ESP32-CAM):

| GPIO | Назначение |
|------|------------|
| GPIO 4 | Светодиод вспышки |
| GPIO 33 | Статусный светодиод |
| GPIO 0 | XCLK |
| GPIO 26 | SDA (SCCB) |
| GPIO 27 | SCL (SCCB) |
| GPIO 32 | PWDN |
| GPIO 25 | VSYNC |
| GPIO 23 | HREF |
| GPIO 22 | PCLK |
| GPIO 5, 18, 19, 21, 36, 39, 34, 35 | D0-D7 (данные камеры) |

## 🌐 Платформы и инструменты

- [PlatformIO](https://platformio.org/) — кроссплатформенная система сборки
- [Arduino Framework](https://www.arduino.cc/) — фреймворк для разработки
- [ESP-IDF](https://docs.espressif.com/projects/esp-idf/) — основной фреймворк ESP32
- [ESP Camera Library](https://github.com/espressif/esp32-camera) — библиотека для работы с камерой

## 🛡️ Безопасность и конфиденциальность

При использовании проекта рекомендуется:
- Изменить учетные данные Wi-Fi по умолчанию
- Использовать защищенные пароли
- Обеспечить безопасность сервера, принимающего изображения
- Рассмотреть использование HTTPS вместо HTTP для передачи данных

## 🤝 Вклад в проект

Мы приветствуем любой вклад в проект! Пожалуйста, ознакомьтесь с файлом [CONTRIBUTING.md](CONTRIBUTING.md) для получения информации о том, как начать и какие правила следовать.

### 🐛 Сообщения об ошибках

Если вы найдете ошибку, пожалуйста, создайте [новое issue](https://github.com/f2re/esp32cam/issues) с подробным описанием проблемы и шагами для воспроизведения.

### ✨ Пожелания функций

Если у вас есть идеи по улучшению проекта, пожалуйста, создайте [новое issue](https://github.com/f2re/esp32cam/issues) с меткой `enhancement`.

## 📜 Лицензия

Этот проект лицензирован под [MIT License](LICENSE.md). См. файл `LICENSE.md` для получения дополнительной информации.

## 🌟 Спасибо

- [Espressif Systems](https://www.espressif.com/) за отличную документацию и инструменты
- [PlatformIO Team](https://platformio.org/) за потрясающую IDE
- Сообществу Arduino за богатую экосистему библиотек
- Laureat за первоначальный код проекта

## 📞 Контакты

Если у вас есть вопросы или предложения, пожалуйста, свяжитесь с нами:

- 💬 GitHub: [@f2re](https://github.com/f2re)
- 🌐 Веб-сайт: https://github.com/f2re/esp32cam

---

<div align="center">

**ESP32-CAM Автоматическая Фотосъемка** © 2025 |
Создано с ❤️ |
[Наверх](#-esp32-cam-автоматическая-фотосъемка-с-загрузкой-на-сервер-)

</div>